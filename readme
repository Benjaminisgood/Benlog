# 🧠 Benlog | 科研博客知识社交等一体化个人网站平台

朋友圈改变了我们的社交方式，给我们带来了便利，但也让我们失去了深度交流的空间。也让分享变得肤浅和碎片化，甚至变味。
这种方式的改变，拉远的人与人距离的同时，还给每个人的带来了额外的社交压力和攀比心理和生活负担。
而 Benlog 希望通过一个轻量化、易部署、易使用的个人网站平台，帮助大家重新找回深度交流和分享的乐趣，摆脱社交网络的束缚，专注于内容本身。
它是一个面向科研工作者、学生与写作爱好者的 **Flask 驱动网站平台**。  
它支持图文博客、知识笔记、动态页面、社交平台等功能，旨在帮助用户轻松创建和管理个人网站，分享知识和经验。

---

## 🔍 网站主要内容结构

| 模块路径 | 内容说明 |
|----------|----------|
| `Dynamic_pages/` | 用 JSON 构建的动态网页 |
| `Index(dynamic_links)` | 外链聚合页面（动态pages以及友情链接） |
| `Blog` | Markdown+html 博客文章 |
| `Edu` | Markdown+html 学习笔记 |
| `Neibr` | 简易论坛样帖子（支持文本+图片） |
| `Gallery` | 图床个人展示一体化 |
| `Index(AI)` | 即可以大语言模型提问，也可以站内搜索，也是个人ai助理 |

---

## 💡 三种内容编辑方式

1. **结构化编辑（JSON）**：灵活强大、功能自由，适合网页定制，适合永久化的pages，搭配动态链接使用，自由定制你的主页快捷卡片，未来会支持导出为各种类型文件（如latex、pdf等）比如你可以直接将aboutme导出为简历使用哦；
2. **Markdown 博客和笔记**：使用 `.md` 编写，简单直观、图文并茂，适合长文章书写，推荐搭配jupyterlab导出的html使用，可以作为你学习工作的展示区域（比如每天在lab里做的笔记或者科研记录，直接导出为html（base64）粘在输入框中展示给导师）没想到吧，让导师当管理员，他还能直接编辑你的笔记呢；
3. **基础文本/媒体拼接**：零依赖纯文本 + 图片发帖，不仅每个用户的帖子数据稳定自由，而且图文视频甚至所有文件类型全部都可以支持，未来再引入ai大模型，自己每天的碎碎念和日常分享就可以在这里进行啦，再也不用担心手机里相册爆满没地方分享又舍不得删的问题啦。


---

## 🚀 快速部署教程（适合 0 基础用户）

### 📦 一键安装运行

```bash
# 下载项目源码
git clone https://github.com/yourname/Benlog.git
cd Benlog

# 建立虚拟环境
python3 -m venv myenv
source myenv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 初始化数据库
flask --app Benlog/app.py db init
flask --app Benlog/app.py db migrate -m "initial"
flask --app Benlog/app.py db upgrade

# 运行本地开发服务器
flask --app "Benlog.app:create_app" run --host=0.0.0.0 --port=80
# 或者
gunicorn -w 3 --threads 6 -b 0.0.0.0:80 "Benlog.app:create_app()"

```

---

### 📦 一键更新
cd /home/Benlogmain.tar/Benlogmain
chmod +x deploy.sh
./deploy.sh





# 如果需要进行版本更新也非常简单，只需在服务器中依次：
cd /Benlogmain
source myenv/bin/activate
chmod +x deploy.sh
./deploy.sh
# 随后再继续一键启动即可。
pkill -f gunicorn
gunicorn -w 3 --threads 6 -b 0.0.0.0:80 "Benlog.app:create_app()"
# setup、manifest也可以帮助你先将程序打包，小白可以直接忽略。
# 测试代码
export FLASK_ENV=development
export FLASK_DEBUG=1
flask --app "Benlog.app:create_app" run --host=0.0.0.0 --port=80

cd /home/Benlogmain.tar/Benlogmain
---

## 🔒 配置说明（可选）

系统默认读取内置配置。如需自定义：

```bash
mkdir instance
touch instance/config.py
```

在 `instance/config.py` 中写入：

```python
# instance/config.py

DEBUG = True
SECRET_KEY = 'your-secret-key'

# 数据库路径（SQLite 示例）
SQLALCHEMY_DATABASE_URI = 'sqlite:///benlog.db'
SQLALCHEMY_TRACK_MODIFICATIONS = False

# 登录管理相关
REMEMBER_COOKIE_DURATION = 3600 * 24 * 7  # 一周自动登录


# OSS 云存储（如果使用）
OSS_ACCESS_KEY_ID = 'your-access-key-id'
OSS_ACCESS_KEY_SECRET = 'your-access-key-secret'
OSS_BUCKET_NAME = 'your-bucket-name'
OSS_ENDPOINT = 'oss-cn-your-region.aliyuncs.com'

```

---

## 如果你要开发，📤 贡献/部署建议

1. 建议新功能用分支开发：
```bash
git checkout -b feature/xxx
```

2. 推送代码：
```bash
git add .
git commit -m "功能更新"
git push origin feature/xxx
```

3. 欢迎大佬加入一起维护完善这个项目

4. 📬 反馈 & 建议欢迎通过 issue 或 email 提出！

5. 这个项目也可以作为实验室的科研工作者们的个人网站平台，欢迎大家使用和贡献。
---

## 🛠 未来功能规划
